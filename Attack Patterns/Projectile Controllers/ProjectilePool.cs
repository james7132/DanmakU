using UnityEngine;
using System.Collections;
using UnityUtilLib;

public class ProjectilePool : GameObjectPool<Projectile, ProjectilePrefab> {
	//This was created purely because Unity hates serializing generic objects
}
